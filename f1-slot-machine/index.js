'use strict';
module.exports = function (settings, opts) {
    //console.log('settings: ',settings,', opts: ',opts);

    settings = settings || {};

    if(!settings.targets) throw new Error('You need to supply f1-slot-machine targets!');



    return settings;
};